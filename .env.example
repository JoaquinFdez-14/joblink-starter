# Example environment variables for local development
# Copy to `.env` and adjust values (DO NOT commit real secrets)

# Node API
API_BASE=http://localhost:8080
AUTH_TOKEN=secreto123

# Django
DJANGO_SECRET_KEY=replace-this-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
API_TOKEN=secreto123

# Firebase service account: path to the JSON key file (keep file out of repo)
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/serviceAccountKey.json
